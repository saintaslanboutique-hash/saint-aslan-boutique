/** Shared with IntroLoader, MobileIntroLoader, and IntroHeader for layoutId morph. */
export const LOGO_CONTAINER_LAYOUT_ID = "logo-container";

/** Mobile intro loader + navbar logo (px); keep in sync across both. */
export const INTRO_MOBILE_LOGO_FONT_PX = 34;
